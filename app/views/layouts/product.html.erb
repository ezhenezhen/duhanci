<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title> | Duhanci</title>
		<% if params[:action] == "show" %>
		<%= stylesheet_link_tag "jquery.fancybox", "main", "product-detail"  %>
		<% else %>
		<%= stylesheet_link_tag "main", "search" %>
		<% end %>
		<%= javascript_include_tag "jquery" %>
		<%= javascript_include_tag "jquery.fancybox" %>
		<%= csrf_meta_tags %>
	</head>
	<body>

		<%= yield %>

	</body>
</html>
